# 평생사주 — 최신화 작업 레이어맵

## 4-Part 분리 (검수 / 프롬프트 / 보강 / 참조)

hongsil/inyeon/parent-child 패턴 그대로 평생사주에 적용.

| 파트 | 메모 | 위치 | 역할 |
|---|---|---|---|
| **검수** | `1_검수파트.md` | `lib/llm-output-guard.ts` saju 분기 | LLM 출력 결정론 가드 + judge/rewrite |
| **프롬프트** | `2_프롬프트파트.md` | `app/api/generate/route.ts` 안 SAJU_PROMPTS 14 섹션 + opener + buildHeader | 메인 prompt + 슬림화 (요청 시) |
| **보강** | `3_보강파트.md` | `lib/personal-traits-block-v2.ts` 등 | 사주 인자 derive + prompt block |
| **참조** | `4_참조파트.md` | hongsil/inyeon/parent-child → 평생사주 자산 매핑 | 어떤 자산 그대로 / 변형 / 미적용 |

## 진행 로그
`5_진행로그.md` — 시간순 작업 기록 + 배포 URL

## 평생사주 특이성

| 항목 | hongsil | inyeon | parent-child | **평생사주** |
|---|---|---|---|---|
| 챕터/섹션 수 | 6 | 8 | 6 + outro | **14 + opener** |
| 라우트 | hongsil-generate | inyeon-generate | /api/generate V2 | **/api/generate L4279** |
| 본문 헤더 | ## + ### | ## + ### | ## + ### | **### 만** (## 없음) |
| Stream 방식 | 청크 누적 → 가드 → 단일 push | 동상 | 동상 | **청크별 push** (글자 떠오름) |
| 검수 가드 | 7종 + judge/rewrite | 동상 | 6종 + judge/rewrite | **0건** ❌ |
| GuardService 타입 | "hongsil" | "inyeon" | "parent-child" | **미등록** |
| 클라이언트 매핑 | 챕터 인덱스 (cs/cd) | 동상 | phase 마커 | **displayPages 단위 통합** |

## 우선순위

1. **Step 1** — GuardService "saju" 등록 + llm-output-guard.ts saju 분기 (hongsil 패턴)
2. **Step 2** — route.ts L4279~ 흐름 변경 (청크 누적 + 가드 + 단일 push)
3. **Step 3** — Cross-chapter usedTokens Map (14 섹션 cross)
4. **Step 4** — 헤더 안전망 (각 섹션 expected ### sub 정의)
5. **Step 5** — 진단 로깅
6. **Phase 0** — 메인 prompt 슬림화 (요청 시)
7. **Phase 2** — 구체화 prompt (요청 시)
