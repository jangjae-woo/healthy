# 5. 진행 로그

## 작업 시간순 기록 — 무엇을 / 언제 / 결과

---

## 2026-05-13 — 폴더·메모 작성 ✓
- `saju/룰북/부모자녀최신화/` 폴더 생성
- 메모 5종 작성:
  - 0_레이어맵_인덱스.md
  - 1_검수파트.md
  - 2_프롬프트파트.md
  - 3_보강파트.md
  - 4_참조파트.md
  - 5_진행로그.md (이 파일)

---

## 2026-05-13 — Phase 1 검수 가드 정합 ✓

**llm-output-guard.ts에 `parent-child` 명시 분기 신규** (inyeon 분기 직후):
1. ✓ `applyHongsilLifestyleRepair` — 결핍 변형·비문 치환
2. ✓ `stripPartnerHallucination` — 가족 호칭은 자연스러우니 약화 적용
3. ✓ `stripSipseongScoreLeakage` — 십성 원점수 strip
4. ✓ `suppressRepeatedHongsilEvidence` — usedTokens 안 넘어와도 챕터 내부 반복은 압축
5. ✓ `stripUserNameSangSuffix` — 아이/엄마/아빠 이름 + "상" → "님"
6. ✓ `judgeOutput` + `rewriteOutput` (ch1~ch6, outro 비활성)
7. ✓ rewrite 후 결정론 가드 한 번 더 (이중 안전망)

**outro 특화**:
- judge/rewrite 비활성 (짧은 시적 단락이라 LLM judge가 톤 망가뜨릴 위험)
- `strip2ndNaturalSimile` 적용

**ch6 특화**:
- judge/rewrite 살림 (사주 풀이 챕터라)
- `strip2ndNaturalSimile` 적용 (가족 한 장면 비유 절제)

**rewriteOutput prompt**:
- `REPETITION_TONE_GUIDE` parent-child에도 주입 (L746 조건 확장)

**route.ts (/api/generate v2)**:
- 가드 후 길이 로깅 추가 (`pre-len=${accumulatedText.length} post-len=${finalText.length}`)
- ch6/outro 진단 로깅 추가:
  - ch6: sub-hits 4종 매칭 카운트 (엄마/아빠/편안한/채워줄)
  - outro: 헤더("자도인의 마지막 당부") + 푸터 disclaimer 존재 체크

**보류 사항**:
- Cross-chapter usedTokens Map — 부모자녀 V2는 phase별 단일 fetch 구조라 인프라 큰 변경 필요. 본문 검수 후 결정.
- ch6/outro 헤더 안전망 (강제 추가) — 아직 빈 응답 사건 없음. 진단 로깅으로 먼저 관측 후 결정.

**배포 URL**: https://saju-m85babvlt-woos-projects-18447b83.vercel.app

---

## 2026-05-13 — Phase 1 회귀 + Binary Search 디버깅 ✓

**증상**: 부모자녀 풀이 모든 sub에 "(이 소제목 본문이 아직 없어요)" fallback. 차트(deterministic)만 표시.

**Binary Search 6회 진행** (격리 → 격리 → 격리):
| 테스트 | 활성 | 결과 |
|---|---|---|
| 1 | REPETITION_TONE_GUIDE 부모자녀 주입 단독 (B) | ✅ 정상 → B 무관 |
| 2 | `suppressRepeatedHongsilEvidence` 단독 | ✅ 정상 → 단독 무관 |
| 3 | `applyHongsilLifestyleRepair` 단독 | ✅ 정상 → 단독 무관 |
| 4 | 6개 함수 다 활성 | ❌ fallback → 조합이 원인 |
| 5 | `lifestyle` + `suppress` 2개만 | ❌ fallback → **이 2개 조합이 원인 확정** |
| 6 | 나머지 4개만 (lifestyle/suppress 빼고) | ✅ 정상 → 최종 운영 결정 |

**최종 코드** (`lib/llm-output-guard.ts` parent-child 분기):
- ✅ `stripPartnerHallucination`
- ✅ `stripSipseongScoreLeakage`
- ✅ `stripUserNameSangSuffix`
- ✅ `strip2ndNaturalSimile` (ch6/outro만)
- ❌ `applyHongsilLifestyleRepair` — 비활성 (lifestyle + suppress 조합 회귀)
- ❌ `suppressRepeatedHongsilEvidence` — 비활성 (동상)
- ❌ judge/rewrite — 우회 (rewrite 호출 자체가 ### 헤더 strip 위험)

**비활성 사유 메모**: 두 함수 단독은 안전. 조합으로 클라이언트 buildSlideTextMap 매핑 실패. 정밀 메커니즘은 추후 (가능성: lifestyle 본문 치환 후 suppress의 ### segment 처리에서 본문 누락). 부모자녀 본문엔 hongsil 결핍 어휘 적게 등장 → 둘 다 비활성 큰 손해 X.

**REPETITION_TONE_GUIDE 부모자녀 주입은 살림** (B는 무관 확정).

**최종 배포 URL**: https://saju-mfzjka08t-woos-projects-18447b83.vercel.app
**총 배포 회수**: 6회 (사용자 검증 + 인내심)

## 2026-05-13 — Step 1 근본 fix (회귀 해결) ✓

**작업**:
1. `suppressRepeatedHongsilEvidence` 함수 안에 preamble 보존 추가 (`lib/llm-output-guard.ts` L545~)
   - `segments.map().join("")` 직전 첫 ### 헤더 이전 본문(`## N장` 챕터 헤더 + intro)을 prepend
   - hongsil/inyeon에 영향 0 (preamble 빈 경우)
2. `rewriteOutput` prompt에 "마크다운 헤더 구조 보존 절대 룰" 추가 (`lib/llm-output-guard.ts` L720~)
   - `## N장`, `### sub 제목` 헤더 strip·변형 절대 금지 명시
   - rewriteOutput이 ### 헤더 떼고 본문 재작성하는 위험 차단
3. parent-child 분기 가드 6종 + judge/rewrite 다 활성 복귀
   - lifestyle / suppress / partner / sipseong / userNameSang / simile(ch6,outro) 6종
   - judge + rewrite (ch1~ch6) + outro만 비활성

**검증**:
- 배포 URL: https://saju-nlwcnhwv4-woos-projects-18447b83.vercel.app
- 사용자 시각 확인: 정상 출력 ✓

**점수 변화 추정**: 부모자녀 75 → 82

## 2026-05-13 — Step 2 클라이언트 매핑 챕터 인덱스 기반 재작성 ✓
- **fetch 루프에 phase 마커 삽입** (`<<<PARENT_CHILD_PHASE:chN>>>`)
- **`buildSlideTextMap` 재작성** — 마커로 phase 분기 → phase → 챕터 매핑 → ### sub만 매칭. ## 헤더 매칭 강제 제거
- **`buildSlideTextMapLegacy` 보존** — 마커 없으면 옛 동작 자동 fallback (안전망)
- 서버 변경 0 (클라이언트 단에서 마커 박음, hongsil/inyeon cs/cd 이벤트 패턴과 동등 기능)
- 배포: https://saju-7ds7udivh-woos-projects-18447b83.vercel.app
- 검증: 모든 sub 정상 출력 ✓ / ch6 동적 sub OK / outro OK / 영상 OK
- 점수: 82 → 89

## 2026-05-13 — Step 3 인프라 3개 추가 (13+14+15) ✓
**13. Cross-chapter usedTokens Map**
- 서버 (`route.ts` V2 분기): `data.usedTokens` 받아 Map 생성 → 가드 mutate → 직렬화해서 `tk` 이벤트 push
- 클라이언트 (`ParentChildSlideResultV2.tsx`): `usedTokens` closure 변수 누적, 매 phase fetch body에 포함, `tk` 이벤트 받아 갱신
- 효과: 같은 한자 토큰이 챕터 경계 넘어 등장 시 3회째부터 풀 대명사 치환 → hongsil/inyeon 동등

**14. 헤더 안전망**
- ch6 expected 3 sub (hasMom/hasDad 기반 동적) 누락 시 본문 끝에 `### {sub}\n(다음 풀이에 이어집니다.)` 강제 추가
- 146 케이스(Gemini 응답 끊김) 재발 시 사용자에게 더 친절한 placeholder 표시 (fallback "본문이 아직 없어요" 대체)
- outro는 sub 없는 단일 단락이라 안전망 X (진단만)

**15. 진단 로깅**
- 모든 phase: `output guard ... pre-len=${...} post-len=${...}`
- ch6: `[pc ch6 diag] sub-hits=3/3 ✓` / `MISSING HEADERS — ...` / `head-150=...`
- outro: `[pc outro diag] has-header=... has-footer=...`

**배포**: https://saju-2olc3wje8-woos-projects-18447b83.vercel.app
**검증**: 사용자 풀이 정상 ✓
**점수**: 89 → 92

## hongsil/inyeon vs parent-child 최종 매트릭스
- 가드 6종 + judge/rewrite + 헤더 보존 룰 + 클라이언트 매핑 + cross-chapter + 헤더 안전망 + 진단 로깅: **모두 동등** ✅
- 부모자녀 특수성으로 못 따라간 것: `normalizeCharacterName` (자도인 1명 = noop), `stripChapter5Hanja` (hongsil 전용 패턴)

## (예정) Phase 0 — 메인 prompt 슬림화 (요청 시)
- 슬림 대상 식별:
- 보존 대상:
- 메모 최신화:

## (예정) Phase 2 — 구체화 prompt (요청 시)
- 강화 sub:
- 신규 파일:
- 라우팅:

## (예정) Phase 3 — 부모자녀 처방 helper (요청 시)
- 신규 자산:
- 사용자 검토:
- 작업:
