# 2. 프롬프트 파트 — 메인 prompt + 슬림화

## 위치
- `app/api/generate/route.ts` 안 ch1~ch6/outro Body (L3162~3350)
- `lib/parent-child-traits-block-v2.ts` (트레이트 → prompt 변환)
- `lib/personal-traits-block-v2.ts` (개인 트레이트)
- `lib/parent-child-charts.ts` (차트 분석)
- `lib/parent-child-compat.ts` (궁합 조정)

## 현재 상태
- ch1~ch6 + outro 7개 phase로 분리, 챕터별 prompt 명확
- 어휘 예시·캐릭터 키워드·매핑 표현 등이 prompt에 박혀 있음 (Phase 0 슬림화 후보)

## Phase 0 슬림화 (예정 — 요청 시)
inyeon Phase 0과 같은 원칙:
- **빼낼 것**: 캐릭터 키워드 예시 / 한자 병기 예시 / 일상 장면 예시 / 매핑 표현
- **보존**: 구조 정의 / 분량 / 한자 룰 / cadence 풀 / 사주 데이터

부모자녀 특화 슬림 후보 (탐색 필요):
- `parent-child-traits-block-v2.ts` 안 어휘 풀
- ch6 어머님/아버님 어휘 예시 (식탁·정돈·실내·따뜻한 손길… / 야외·운동·만들기…)
  - **다만**: 이건 prompt 구조의 핵심이라 슬림 X (사용자 명시 결정 필요)
- ch1~ch5 매핑 표현 (있다면)

## 2026-05-13 — rewriteOutput prompt 강화 ✓

**작업 위치**: `lib/llm-output-guard.ts` L720~ (`rewriteOutput` 함수 안 prompt)

**추가된 절대 룰** (모든 서비스 공통 — hongsil/inyeon/parent-child):
```
[★★★★★ 마크다운 헤더 구조 보존 절대 룰 — 클라이언트 매핑의 생명선]
- 원문에 있던 모든 마크다운 헤더(## N장 — ..., ### sub 제목)는 결과 본문에도
  반드시 동일 위치·동일 텍스트로 유지.
- 헤더 라인 자체를 strip·변형·생략·번역·축약하지 말 것. 단 한 글자도.
- ### sub 헤더 사이 본문만 재작성하라.
- 헤더 누락 시 클라이언트가 본문 통째 매핑 실패해서 사용자에게
  "(이 소제목 본문이 아직 없어요)" 표시됨. 출력 자체가 폐기되니 반드시 보존.
```

**영향**:
- hongsil/inyeon — rewrite 시 헤더 더 안전 (강화). 회귀 위험 0.
- parent-child — rewrite 활성 가능 (이 룰 + suppress preamble fix 둘 다 필요)

## Phase 2 구체화 (예정 — 요청 시)
hongsil refinement 5종 패턴:
- 부모자녀 특화 sub 식별 후 강화 prompt 추가
- 신규 폴더 후보: `lib/parent-child/prompts/refinement/`

## 진행 로그
(작업 진행 시 여기 기록)
