# 1. 검수 파트 — LLM 검수 가드

## 위치
`lib/llm-output-guard.ts` — `parent-child` 분기 (신규)

## 현재 (적용 전)
- `GuardService` 타입에 `"parent-child"` 이미 등록 (L7)
- `/api/generate` route.ts L4063에서 `service: "parent-child"` 전달
- 그러나 **명시적 분기 없음** → default 흐름으로 떨어짐:
  - `applyDeterministicRepair` (SOFT_REPLACEMENTS 기본)
  - `findDeterministicIssues` + `findRepeatedEvidence` 호출
  - `judgeOutput` → `rewriteOutput` 호출
  - 마지막 `applyDeterministicRepair` 한 번 더
- **hongsil이 거친 특화 가드 7종 + REPETITION_TONE_GUIDE 미적용**

---

## Phase 1 작업 — 가드 정합 (회귀 위험 0)

### 추가할 가드 셋 (hongsil/inyeon 정합)

| # | 가드 | 출처 | 부모자녀 적용 |
|---|---|---|---|
| 1 | **suppressRepeatedHongsilEvidence** (usedTokens Map) | hongsil | Cross-chapter 한자 2회 허용, ch2~ch5에 적용 (ch1·ch6·outro 제외) |
| 2 | **stripSipseongScoreLeakage** | hongsil | 십성 원점수 strip (그대로) |
| 3 | **applyHongsilLifestyleRepair** | hongsil | 결핍 변형·비문 치환 (그대로 — "옅게 자리한" 톤 부모자녀도 동일) |
| 4 | **REPETITION_TONE_GUIDE** rewrite prompt 주입 | hongsil | L746 조건에 parent-child 추가 |
| 5 | **헤더 안전망** | hongsil ch6 / inyeon ch8 | ch6 3 sub + outro 1 sub 누락 시 강제 추가 |
| 6 | **진단 로깅** | hongsil ch6 / inyeon ch8 | ch6/outro 단계별 길이 vercel logs 캡처 |
| 7 | **stripUserNameSangSuffix** | inyeon Phase 1.5 | 아이/엄마/아빠 이름 + "상" → "님" (자도인만 캐릭터) |

### 검토 필요 가드 (변형 또는 미적용)

| 가드 | 적용 여부 | 이유 |
|---|---|---|
| `stripPartnerHallucination` | **부모자녀 특화로 수정** | 가족 셋 모두 사주 입력. "상대의 ○○ 기운" 환각은 가족 호칭("엄마/아빠")에선 자연스러우니 약화. 다만 가족 사주에 없는 신살·기운 단정만 차단 |
| `normalizeCharacterName` | **자도인 1명만** | 12 캐릭터 매핑 없음. 자도인 한자 표기 정규화만 — 다만 자도인은 보통 한자 표기 안 쓰므로 사실상 noop |
| `strip2ndNaturalSimile` | **ch6 + outro 적용** | 부모자녀 마지막 챕터·outro 자연 비유 절제 |
| `ch6 rewrite 비활성` | **outro만 비활성** | inyeon ch8과 달리 부모자녀 ch6은 사주 풀이 챕터라 rewrite 살림. outro는 짧은 시적 단락이라 rewrite 비활성 |

---

## 진행 로그

### 2026-05-13 — Phase 1 + Binary Search 디버깅 ✓

**최종 적용 (4종 + ch6/outro 자연 비유 strip)**:
- `stripPartnerHallucination` — 가족 호칭 환각 가드
- `stripSipseongScoreLeakage` — 십성 원점수 strip
- `stripUserNameSangSuffix` — 사용자 이름 + "상" → "님"
- `strip2ndNaturalSimile` — ch6/outro 자연 비유 2개 이상 시 1개만 보존
- judge/rewrite **우회** (default 분기 안 거침)

**비활성 (회귀 사유)**:
- `applyHongsilLifestyleRepair` — 단독 안전, suppress와 조합으로 매핑 실패
- `suppressRepeatedHongsilEvidence` — 단독 안전, lifestyle과 조합으로 매핑 실패
- judge/rewrite — rewriteOutput이 부모자녀 prompt 구조(### 3 sub) 모르고 헤더 strip 위험

**REPETITION_TONE_GUIDE 부모자녀 주입은 살림** (이 자체는 무관 확정 — 다만 rewrite 우회로 사실 활성 안 됨)

**최종 배포 (Step 0)**: https://saju-mfzjka08t-woos-projects-18447b83.vercel.app
**총 배포**: 6회 (binary search)

---

### 2026-05-13 — Step 1 근본 fix (회귀 해결) ✓

**근본 원인 (binary search 결과 + 코드 분석)**:
- `suppressRepeatedHongsilEvidence` 함수의 `segments.map().join("")` 동작이 **첫 ### 헤더 이전 본문을 통째 잘라먹음** = `## N장` 챕터 헤더 + intro 다 사라짐
- hongsil/inyeon은 클라이언트가 챕터 인덱스로 매핑해서 영향 0
- parent-child는 클라이언트 `buildSlideTextMap`이 `## N장` 헤더 강제 매칭 → preamble 잘리면 본문 통째 폐기 → 모든 sub fallback

**fix 2건**:
1. **`suppressRepeatedHongsilEvidence` preamble 보존** (`lib/llm-output-guard.ts` L545~)
   ```ts
   const firstHeadIdx = headings[0].index ?? 0;
   const preamble = firstHeadIdx > 0 ? text.slice(0, firstHeadIdx) : "";
   return preamble + segments.map(...).join("");
   ```
2. **`rewriteOutput` prompt 헤더 보존 절대 룰** (`lib/llm-output-guard.ts` L720~)
   - `## N장`, `### sub 제목` strip·변형·축약 절대 금지 명시
   - hongsil/inyeon에도 안전 강화

**부모자녀 분기 복귀 (현재 활성)**:
- ✅ `applyHongsilLifestyleRepair` — 결핍 변형 부드럽게
- ✅ `stripPartnerHallucination`
- ✅ `stripSipseongScoreLeakage`
- ✅ `suppressRepeatedHongsilEvidence` — preamble 보존 fix 후 안전
- ✅ `stripUserNameSangSuffix`
- ✅ `strip2ndNaturalSimile` (ch6/outro)
- ✅ `judgeOutput` + `rewriteOutput` (ch1~ch6) — 헤더 보존 절대 룰로 안전
- ✅ outro만 rewrite 비활성

**검증**:
- 배포: https://saju-nlwcnhwv4-woos-projects-18447b83.vercel.app
- 사용자 시각 확인: 정상 출력 ✓

**점수 추정**: 75 → 82

---

### 2026-05-13 — Step 2 클라이언트 매핑 챕터 인덱스 기반 ✓
- `ParentChildSlideResultV2.tsx` fetch 루프에 phase 마커(`<<<PARENT_CHILD_PHASE:chN>>>`) 삽입
- `buildSlideTextMap` 재작성 — 마커로 phase 분기 + ### sub만 매칭. ## 헤더 매칭 강제 제거
- `buildSlideTextMapLegacy` 보존 (회귀 안전망)
- 점수: 82 → 89
- 배포: https://saju-7ds7udivh-woos-projects-18447b83.vercel.app

---

### 2026-05-13 — Step 3 인프라 3개 추가 ✓ (hongsil/inyeon 동등 도달)

**Cross-chapter usedTokens Map**
- 서버: `data.usedTokens` 받아 Map 생성 → 가드 mutate → 직렬화해서 `tk` 이벤트 push
- 클라이언트: closure 변수 누적, 매 phase fetch body 포함, tk 이벤트 받아 갱신
- 효과: 챕터 경계 넘어 한자 토큰 3회째부터 풀 대명사 치환

**헤더 안전망**
- ch6 expected 3 sub (hasMom/hasDad 동적) 누락 시 본문 끝에 `### {sub}\n(다음 풀이에 이어집니다.)` 강제 추가
- 146 케이스(Gemini 응답 끊김) 재발 시 친절한 placeholder 표시
- outro: sub 없는 단일 단락이라 안전망 X (진단만)

**진단 로깅**
- 모든 phase: `output guard ... pre-len/post-len`
- ch6/outro: `head-150` / `sub-hits=3/3` / `has-header/has-footer`

**검증**: 사용자 풀이 정상 ✓
**점수**: 89 → 92
**배포**: https://saju-2olc3wje8-woos-projects-18447b83.vercel.app
