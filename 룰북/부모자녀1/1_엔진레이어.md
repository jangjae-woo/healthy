# 1. 엔진 레이어 — 자녀·부모 사주 + 궁합 계산

## 역할
자녀(필수) + 엄마(옵션) + 아빠(옵션) 생년월일시를 사주 원국·오행 분포·십성·신살·대운·신강신약·용신/희신/기신·격국 + 부모-자녀 궁합 점수까지 모두 결정론 계산. LLM 입력 전 단계.

## 주요 파일
### 단일 사주 (4 서비스 공통)
- `lib/saju-core.ts` — 핵심 계산 진입점 (`computeFullSajuCore`)
- `lib/saju-calculator.ts` — 천간·지지·오행·십성 + `getYongsin`(G15 fix) + `getDayMasterStrength` + `calcCompatibility`
- `lib/saju-traditional.ts` — 격국·공망·기신 (`calcGyeokguk`, `calcGisin`)
- `lib/jpjjeon.ts` — 자평진전 격국 8 정격 + 외격
- `lib/sibiunseong.ts` — 십이운성
- `lib/tonggeun.ts` — 지장간 통근

### 부모자녀 전용
- `lib/parent-child-compat.ts` — `adjustForParentChild` 부모-자녀 궁합 점수 조정
- `lib/parent-child-traits.ts` — `pickFamilySajaSeongeo`·`pickFamilyTrioSaja` 가족 사자성어 + gift card·synergy card
- `lib/parent-child-charts.ts` (·`-v2`) — 8지능·직업·친구 스타일·훈육 채널·digital gauge·TantrumBars·DaeunTimeline 차트 데이터
- `lib/parent-child-traits-block-v2.ts` — `deriveChildTraitsV2` + `childTraitsToPromptBlockV2` (cell framework)
- `lib/parent-child-traits-v2.ts` — `inferYongsinMeaning`(G15 fix)·`inferDominantMeaning`·`getIljuInfo`·`inferPositiveSinsal`
- `lib/age-stage.ts` — 연령대 분류 + `ageToneGuide` + `dailyDigitalLimit`
- `lib/parent-child/yongsin-prescription.ts` — Step A 부모자녀 톤 처방 helper

## 진입점
- `app/api/generate/route.ts` L3934~ `type === 'parent-child' || 'parent-child-v2'` 분기
- `computeFullSaju(year, month, day, hour, isLunar, gender)` 호출
- V2: `phase === "compute"` → 사주 메타 JSON 응답 / 다른 phase → LLM 생성

## 출력 데이터
- 자녀: `sajuChild` (SajuAnalysisCore) — `pillars`·`ilgan`·`sipseong`·`elements`·`yongsin`·`sinsal`·`daeun.cycles`·`shinkang`
- 엄마: `sajuMom` (옵션)
- 아빠: `sajuDad` (옵션)
- 궁합:
  - `momCompat` / `dadCompat` — `adjustForParentChild(calcCompatibility(sajuMom/Dad, sajuChild))`
  - `momCompare` / `dadCompare` — `inferElementCompare` (닮은 결·다른 결)
  - `familySaja` — trio면 `pickFamilyTrioSaja`, dyad면 `pickFamilySajaSeongeo`

## V2 compute phase 응답 (JSON)
```ts
{ sajuMom, sajuDad, sajuChild, momCompat, dadCompat, hasMom, hasDad }
```
→ 클라이언트가 시각 카드 38개 렌더링 + 다음 phase fetch

## 주의
- saju-core·saju-calculator는 4 서비스 공통 — 변경 시 hongsil/inyeon/saju 모두 영향
- `getYongsin`은 2026-05-14에 신강신약 분기 fix (G15) — 신강 케이스에 잘못된 비겁·인성 출력 버그 해결
- `inferYongsinMeaning`은 2026-05-14에 saju.yongsin 우선 사용 fix — 222 모순 해결

## G15 시각화 데이터 정합 (2026-05-14)
- **222 이미지 모순** (채워줄 결=화 + 살펴줄 결=화) 원인:
  - `inferYongsinMeaning`이 saju.yongsin 무시하고 자체 "가장 부족한 오행" 사용
  - `calcGisin`은 saju.yongsin 기반 (ELEM_OVERCOMING_REVERSE)
  - 두 함수 yongsin 정의 충돌 → 우연히 같은 오행 출력
- fix: `inferYongsinMeaning`이 saju.yongsin 우선 사용 → 두 함수 정합

## 자동 회귀에서 메타 sanity check
- `peanut/saju_test.js`의 `runMetaSanity(sajuChild)` 함수
- 점검 항목:
  - yongsin이 오행 글자인지
  - yongsin ≠ gisin (overcoming-reverse 적용)
  - inferYongsinMeaning weakest ≠ calcGisin element (222 케이스)
  - shinkang 텍스트 분류 정상

## 진행 로그
- 2026-05-14: getYongsin 신강신약 분기 fix (G15) + inferYongsinMeaning fix (222 모순) + 자동 회귀에 메타 sanity 추가
- 2026-05-13: 자도인 V2 cell framework 도입
- 2026-05-10: V2.5~V2.7 자평진전 정통 framework 대규모 도입
