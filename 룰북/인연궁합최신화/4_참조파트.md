# 4. 참조 파트 — 연애사주 자산 → 인연궁합 매핑

## 연애사주(hongsil) 자산 목록

### 검수 가드 (`lib/llm-output-guard.ts`)
| hongsil 자산 | 출처 라인 | inyeon 적용 |
|---|---|---|
| `suppressRepeatedHongsilEvidence` | L450~ | ✓ Phase 1 |
| `stripSipseongScoreLeakage` | L380~ | ✓ Phase 1 |
| `SOFT_REPLACEMENTS` 변형 (Fix E·E2) | L99~115 | ✓ Phase 1 |
| `REPETITION_TONE_GUIDE` rewrite 주입 | L728 | ✓ Phase 1 |
| `normalizeCharacterName` | L412~ | △ inyeon 2 캐릭터 적용 |
| `strip2ndNaturalSimile` | L430~ | △ ch8 적용 (ch6 패턴) |
| `stripChapter5Hanja` | L395~ | △ ch별 정책 확인 후 |
| `stripPartnerHallucination` | L335~ | ✗ inyeon은 2명이라 의미 다름 — 별도 검토 |
| ch6 rewrite 비활성 | L757 | △ ch8 적용 |
| 헤더 누락 안전망 | route L178~ | ✓ ch8 적용 |
| 진단 로깅 (ch6) | route L179~ | ✓ ch8 적용 |

### 메인 prompt 슬림화 패턴 (Phase 0)
| hongsil 위치 | inyeon 대응 |
|---|---|
| `interpretation-plan.ts` L187 | inyeon은 interpretation-plan 없음 — sub-distribution.ts·v2-principles.ts 검토 |
| `v5-report.ts` L65 | `lib/inyeon/prompts/ch1~ch8.ts` + `v2-principles.ts` |
| `rewriteOutput` L646~657 | 같은 파일 inyeon 분기에도 적용 (hongsil 패턴 그대로) |
| `route.ts` crossChapterBlock | `inyeon-generate/route.ts`에 crossChapterBlock 신규 |

### 구체화 prompt 패턴 (Phase 2)
| hongsil refinement | inyeon 후보 |
|---|---|
| `solo-escape.ts` | (해당 없음 — 두 명 풀이라 솔로 컨셉 X) |
| `yearly-flow.ts` | inyeon ch5 또는 ch7에 "올해 둘의 흐름" 가능 |
| `inner-charm.ts` | inyeon ch3 성격 / ch5 본능에 두 사람 매혹 가능 |
| `good-partner-signal.ts` | inyeon 의미 다름 — 두 명 이미 정해진 상태 |
| `repetition-tone.ts` | ✓ rewrite에 그대로 주입 (Phase 1) |

### helper / 매핑 자산 (Phase 3)
| hongsil 자산 | inyeon 대응 |
|---|---|
| `yongsin-prescription.ts` | `inyeon-prescription.ts` 신규 — 두 사람 함께 처방 |
| `STEM_DEEP` (일간 본질) | 그대로 활용 가능 — 두 사람 각자 |
| `BRANCH_DEEP` (배우자궁) | 그대로 활용 — 두 사람 각자 |
| `SHINKANG_DEEP` (7단계) | 그대로 활용 — 두 사람 각자 |
| `LOVE_THERMOMETER_INFO` | inyeon에 두 사람 사랑 온도 비교 가능 |

### 시각화 컴포넌트 (page.tsx 차원 — 별도 작업)
사용자 의견: **"인연궁합 시각화는 더 훌륭하다"** → 시각화는 그대로 유지. 본문 LLM만 hongsil 패턴으로.

---

## 작업 원칙

1. **연애사주 자산을 그대로 가져올 때** — 참조 출처 명시 (이 파일에 기록)
2. **수정해서 가져올 때** — 수정 사유 + 수정 내용 기록
3. **신규로 만들 때** — 인연궁합 특화 이유 기록

→ 나중에 둘 사이 다시 변경 발생 시 추적 가능.

---

## 진행 로그

(작업 진행 시 여기 기록 — 어떤 hongsil 자산을 언제 inyeon에 가져왔는지)

### YYYY-MM-DD
- ...
