# 인연궁합 ch3 — 3장 성격궁합 (소제목 8개)

⚠️ 수정 전 `0_인연궁합_읽어주세요.md` 먼저.

- **파일**: `lib/inyeon/prompts/ch3-seonggyeok.ts`
- **함수**: `buildInyeonChapter3Prompt(req, c)` — `c`는 SeongCtx
- **편집 구역**: 반환 백틱 템플릿 안 — 페르소나 / `[★★★ 핵심 룰]` / `[★ 3장 출력 룰]` / 4개 `##` 헤더 / `### 소제목` 8개 블록
- 소제목 블록은 **템플릿 리터럴 직접**

## 변수 (보존)
`${c.aName}` `${c.bName}` `${c.aIlgan}` `${c.bIlgan}` `${c.ilganRelation}` `${c.aOhaengTop}` `${c.bOhaengTop}` `${c.aOhaengWeak}` `${c.bOhaengWeak}` `${choiceCtx}`
조건부: `${c.aCharacter && c.bCharacter ? \`...\` : ""}` (캐릭터 결합 데이터 블록 — 삼항 통째 보존), `${c.pairLabel}` `${c.pairTone}`

## 고정 헤더 (변경 금지)
- `## 우리의 성격 케미` / `## 우리의 끌림 포인트` / `## 함께 오래갈 수 있을까` / `## 성격 궁합을 보완하는 법`
- `### 우리 성격이 만났을 때의 큰 그림` `### 함께 있을 때 자연스럽게 나뉘는 역할` `### 그 사람이 나에게 느끼는 매력` `### 내가 그 사람에게 느끼는 매력` `### 시간이 지나도 단단할 수 있을까` `### 누가 주도하고 누가 양보하는가` `### 그 사람이 나를 더 좋아하게 만드는 법` `### 우리가 함께할 때 가장 잘 맞는 활동`

## 소제목 구성
| # | 소제목 | 메인 인자 | 시그너처 |
|---|---|---|---|
| 1 | 우리 성격이 만났을 때의 큰 그림 | 일간 관계 | 자연 풍경 비유형 |
| 2 | 함께 있을 때 자연스럽게 나뉘는 역할 | 일간 음양·신강신약·비겁·관성 | 두 갈래 분담 묘사형 |
| 3 | 그 사람이 나에게 느끼는 매력 | a 일간·강한 오행·매력 신살 | 단정+일화형 (b→a 시점) |
| 4 | 내가 그 사람에게 느끼는 매력 | b 일간·강한 오행·매력 신살 | 단정+일화형 (a→b 반대 시점) |
| 5 | 시간이 지나도 단단할 수 있을까 | 합·생·충·원진·해 | 시간 흐름 양면형 |
| 6 | 누가 주도하고 누가 양보하는가 | 일주 음양·식상·재성 | 두 갈래 비교형 (※ 비겁/관성 메인 다시 X) |
| 7 | 그 사람이 나를 더 좋아하게 만드는 법 | b 부족한 오행·약한 결 | 처방전 산문형 |
| 8 | 우리가 함께할 때 가장 잘 맞는 활동 | 두 사람 용신·강·약 오행 보완 | 분위기 풍경화형 |

분량 320~480자. 점수·등급·% 금지. 6번 블록 `※` 인자 회전 주석 유지.
