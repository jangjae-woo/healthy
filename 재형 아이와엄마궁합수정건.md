# 대화 로그 — 부모-자녀 사주 페이지 개선
**기간:** 2026-04-29 ~ 2026-04-30
**도메인:** paljawon.com (Vercel 프로젝트 `saju`)
**주요 작업 영역:** 부모-자녀 궁합 페이지 (`/parent-child/result`)

---

## 0. 환경 설정

### 0.1 프리뷰 서버
- 처음 `Next.js dev (saju)` 포트 3456 시도 → 이미 다른 dev(`saju-dev`)가 점유
- `.claude/launch.json` port 5555로 변경 후 새 서버 기동 → http://localhost:5555

### 0.2 사용자 컨텍스트
- 사용자 이름: 장재우 (jpvision89@gmail.com)
- GitHub: jangjae-woo
- Vercel: tooxx3-1879 계정 (다른 계정)

---

## 1. "엄마와 아이 궁합" 페이지 누락 → 복원

### 1.1 진단
- 워크트리(`.claude/worktrees/vibrant-cerf-1390b8`)에 `parent-child` 라우트 없음
- master saju (`C:/Users/PC/Desktop/saju/`)에는 존재
- 워크트리는 별도 브랜치(`claude/vibrant-cerf-1390b8`)라 동기화 안 됨

### 1.2 복원 — 워크트리에 master saju 파일 복사
복사한 파일:
- `app/parent-child/page.tsx`
- `app/parent-child/form/page.tsx`
- `app/parent-child/result/page.tsx`
- `components/ParentChildSlideResult.tsx`
- `lib/parent-child-traits.ts`
- `lib/parent-child-observation.ts`
- `lib/parent-child-compat.ts`
- `lib/parent-child-charts.ts`

### 1.3 메인 페이지에 카드 추가
[`app/page.tsx`](app/page.tsx) services 배열에 entry 추가:
```ts
{
  id: "parent-child",
  hanja: "慈",
  en: "Mother & Child",
  title: "엄마와 아이 궁합",
  desc: "사랑으로 이어진 두 사람의 결을 풀어드립니다",
  href: "/parent-child",
}
```

---

## 2. 배포 워크플로우 정립

### 2.1 초기 시도 — git push
- 원격 `jangjae-woo/saju-site.git`로 push 성공
- 그런데 Vercel 대시보드에 새 배포 안 뜸
- 4시간째 같은 빌드(`81451dc`) 유지

### 2.2 진짜 원인 발견
- Vercel은 다른 repo(이름 `saju`)를 watching
- master saju의 `.git`은 `saju-site` 가리킴 → 우리가 푸시한 곳 ≠ Vercel이 보는 곳
- `jangjae-woo/saju`는 Initial commit만 있는 빈 스캐폴드

### 2.3 해결 — Vercel CLI 직접 배포
- vercel CLI가 tooxx3-1879 계정으로 이미 로그인되어 있음
- `npx vercel --prod --yes` — git 우회, 현재 working dir 그대로 production 배포
- 54초~1분 만에 paljawon.com 반영

**최종 배포 명령:**
```bash
cd C:/Users/PC/Desktop/saju
npx vercel --prod --yes
```

### 2.4 Force push 차단 정책
- 다른 PC에서의 `git push --force`로 한 번 커밋 사라짐
- 메모리에 영구 규칙 저장: `feedback_no_force_push.md`
- GitHub Branch Protection 권장

### 2.5 토큰 노출 경고
- git remote URL에 `ghp_...` PAT 박혀있음
- 사용자가 화면 공유·로그 보관 시 노출 위험
- 사용자는 private 레포라 안전하다고 판단, 그대로 유지

---

## 3. "가족 인연의 결" 한 줄 카피 — 5단계 반복 개선

### 3.1 1단계 — 기본 패턴 → 호칭 추가
**문제:** "엄마와 아이의 본질이 만나는 결: 김수지이 전아인을 곧게 단련시켜 주는 사이"
- 받침 없는 "김수지" 뒤 "이" 어색 (정상은 "가")
- "양" 호칭 빠짐

**해결:**
- [`lib/wording.ts`](lib/wording.ts) `withChildHonorific(name, gender)` 추가
- "여" → name + "양" / "남" → name + "군"

### 3.2 2단계 — 라벨 변경 + 한 줄 카피
**문제:** 헤딩 "엄마와 아이의 본질이 만나는 결"이 부모→아이 순서를 암시하는데, 본문은 오행 방향 따라 [아이][엄마] 순으로 나와 거꾸로 보임

**해결:**
- 라벨: "○○와 아이의 본질이 만나는 결: ..." → "○○와 아이의 궁합: ..."
- `parentChildOneLiner(compat)` 함수 — 부모가 채워주는 오행(`aHelpsB[0]`)으로 동사구 매핑

### 3.3 3단계 — 엄마/아빠 출력이 동일한 문제
**문제:** 같은 가족에서 엄마·아빠 둘 다 같은 오행을 채워줄 때 출력이 동일

**해결 (A+B 혼합):**
- A: 일간 관계로 1차 분기 (비화/상생/상극)
- B: 부모 역할별 동사·결 어휘 차별화 (엄마=정서적, 아빠=방향성)

### 3.4 4단계 — "단단한 손길로 단단한 의지" 중복 어색
**문제:** child trait "단단한 의지"(토) + mom gift "단단한 손길"(금) → "단단한" 두 번

**해결 (안 A+):**
- 토 trait: "단단한 의지" → "묵직한 의지"
- 수 trait·dad gift 충돌 회피: "깊은 분별" → "고요한 분별"

### 3.5 5단계 — 직관성 강화 (안 1+5 혼합)
앞부분 똑같은 문제 → **구조 자체 분리**:
- 엄마: "[gift]으로 [trait]을 [verb] 사이" — 도구격 시작 (정서적)
- 아빠: "[trait]에 [gift]을 [verb] 사이" — 처소격 시작 (방향성)

**최종 매트릭스:**

| 부모 채움 | 엄마 (gift, verb) | 아빠 (gift, verb) |
|---|---|---|
| 목 | 유연함, 부드럽게 풀어주는 | 새로운 길, 활짝 열어주는 |
| 화 | 따스한 온기, 살며시 데워주는 | 환한 등불, 앞에서 비춰주는 |
| 토 | 포근한 품, 가만히 감싸안는 | 든든한 기둥, 단단히 세워주는 |
| 금 | 단단한 손길, 조용히 다독이는 | 곧은 방향, 흔들림 없이 잡아주는 |
| 수 | 잔잔한 결, 살며시 가라앉혀주는 | 고요한 분별, 차분히 일러주는 |

**Child trait (일간 오행 기반):**
- 목 → 뻗어가는 마음
- 화 → 뜨거운 마음
- 토 → 묵직한 의지
- 금 → 날카로운 결단
- 수 → 깊이 잠긴 마음

**조사 처리 헬퍼:**
- `withSubjectParticle(name)` — 받침 따라 이/가
- `withObjectParticle(s)` — 받침 따라 을/를
- `withInstrumentalParticle(s)` — ㄹ 받침 또는 무받침은 로, 그 외 으로

---

## 4. 자도인 첫마디 프롬프트 개선

### 4.1 "한 박자 빠르게/늦춰" 클리셰 진단
- [route.ts:1151,1156](app/api/generate/route.ts:1151) 에 예시 문장이 박혀있어 AI가 거의 그대로 복붙
- 모든 가족 사주에 무관하게 같은 메타포 등장

### 4.2 해결 (안 ① + ③ 결합)
**[`lib/opener-seed.ts`](lib/opener-seed.ts) 신규 — 결정론 시드:**
- 10간 자연 비유 사전 (갑→곧게 뻗는 큰 나무 등)
- 10성 톤 사전 (정인→수용하고 기다리며 길러내는 결 등)
- `buildOpenerSeed(parent, child, role, name, name, helpsElement)` — 시드 객체 빌드

**프롬프트 교체** [route.ts:1152+](app/api/generate/route.ts:1152):
- 5단계 흐름 + "한 박자" 예시 모두 제거
- `[결 데이터 — 엄마와 아이]` 시드 블록 주입
- 엄격 금지 규칙 추가: "한 박자/늦춰/속도/기다려주는 마음" 통상 메타포 절대 사용 금지
- "위 [결 데이터]에서 도출되지 않는 일반론·바넘식 표현 금지"

---

## 5. 자녀 호칭 일관성 강제 (양/군)

### 5.1 문제
첫마디에 "김자인의 결", "김자인를 다듬어" 등 호칭 빠진 표현 다수

### 5.2 해결 (A + B 결합)

**옵션 A — 프롬프트 강화** [route.ts](app/api/generate/route.ts):
- "이름만 OK" 면제 조항 제거
- "단 한 번도 빠짐없이 호칭 부착" 강제
- 받침 조사(을/은/이/의) 명시

**옵션 B — 후처리 안전망** [`lib/text-postprocess.ts`](lib/text-postprocess.ts) 신규:
```ts
ensureChildHonorific(text, childName, childGender)
```
- 단독 "김자인" → "김자인양" 자동 부착 (lookahead로 이중 부착 방지)
- 잘못된 조사 보정: 를→을, 는→은, 가→이, 와→과
- SSE 스트림 [DONE] 시점에 한 번 적용 ([ParentChildSlideResult.tsx:2470](components/ParentChildSlideResult.tsx:2470))

---

## 6. 차트 시각화 — 절대 스케일 + 그리드 라벨

### 6.1 ElementsRadar (오행 5각 차트)
**문제:** 木 48%인데 꼭짓점에 박혀 100%처럼 보임 (상대 스케일)

**해결** [parent-child... 1739](components/ParentChildSlideResult.tsx:1739):
```diff
- const raw = (adjusted[el] || 0) / maxVal;  // 상대
+ const raw = (adjusted[el] || 0) / 100;     // 절대
```
+ 25%/50%/75%/100% 그리드 라벨 추가

→ 木 48% → 절반 위치, 7% → 중심 가까이 (직관적).

### 6.2 SipseongRadar (십성 5각 차트)
**1차 — 절대 스케일** + 그리드 라벨 (8 = 100% 기준).

**2차 — 0인 결 처리 (방사형 막대)**
폴리곤 연결 라인 → **개별 막대 5개** (`<line>`). 0인 결은 막대 자체 안 그림.

```diff
- <polygon points={dataPts} ... />
+ {ORDER.map((k, i) => {
+   if (counts[k] === 0) return null;
+   ...
+   return <line ... strokeWidth="6" strokeLinecap="round" />;
+ })}
```

### 6.3 재성 라벨 정비
- "현실·관리" → **"돈·물건의 결"** ([lib/parent-child-charts.ts:233](lib/parent-child-charts.ts:233))
- "나의 손에 쥐는 결" → **"손에 잡히는 결"** ([ParentChildSlideResult.tsx:1814](components/ParentChildSlideResult.tsx:1814))
- 약함 표현: "이상·꿈에 더 끌리고 현실 감각은 천천히 자랄" → "손에 잡히는 결과보다 머릿속 이상에 끌림 — 결과 챙김이 늦은 편"

---

## 7. AI 분포 윤색 방지 (균형 단어 차단)

### 7.1 문제
"쇠 7% / 물 14%"가 둘 다 약함인데 AI가 "균형을 이뤄 안정적"으로 윤색

### 7.2 해결
**[`lib/opener-seed.ts`](lib/opener-seed.ts) 분류 함수 추가:**
- `classifyElementDistribution(elements)` — 강(≥25%)/중(15~24%)/약(<15%) + 균형 여부(±5%p)
- `classifySipseongDistribution(counts)` — 강(≥2)/중(1~1.99)/약(<1) + 균형 여부(차이 ≤1.5)

**프롬프트 주입** [route.ts:962+](app/api/generate/route.ts:962):
```
[아이 오행 분포 (100% 기준)]
- 강함: 木(자라남) 48%
- 중간: 土(안정) 24%
- 약함: 水 14% · 火 7% · 金 7%
- 전체 균형 여부: 불균형 — '균형' 단어 사용 금지

★ "균형/안정/조화" 단어 사용 조건 명시
★ 약함 카테고리 결 묶어 "둘이 균형"이라 묘사 금지
★ 약함은 약함으로 정직하게 표현
```

기질 5각도 / 10가지 성향의 지도 섹션 규칙 강화.

---

## 8. "표현의 서둠" 오타 → 키워드 규칙 강화

### 8.1 진단
- 코드 하드코딩 0건 — AI 출력
- 프롬프트 `**키워드(3~6글자)**` 제약에 AI가 "표현의 서투름"(7자)을 6자에 맞추려 "서둠"으로 잘라버림

### 8.2 해결 (① + ②)
- ① 글자수 완화: `3~6글자` → `3~8글자` (전 7곳)
- ② 표준어 강제 규칙 추가:
```
★★ **표준 한국어 사전 단어·구만 사용**. "서둠/뭐둠/끄둠" 같은 임의 줄임말·신조어 금지.
   8글자에 안 맞으면 다른 표준 단어로 대체 (예: "표현의 서투름"이 7자라 그대로).
```

---

## 9. 회복 카드 — 사주 보충 처방 매트릭스 (옵션 D)

### 9.1 문제
- 6개 카드(자녀 3 + 부모 3) 내용을 AI가 자유 생성
- 사주 근거 거의 없음 (일반 양육 상식 수준)

### 9.2 설계 — 5×3×6×3 = 270 처방 풀
**축:**
- 5오행 (목·화·토·금·수)
- 3 발달 단계 (유아 0-3 / 아동 4-9 / 청소년 10+)
- 6 카드 (즉효·일상·피해야할결 / 공간·감각·리듬)
- 3 변형 (사주 해시로 선택)

**[`lib/element-prescription.ts`](lib/element-prescription.ts) 신규 (~450줄):**
- `M[element][tier][card]` — 3변형 풀
- `classifyAgeTier(age)` — 만 나이로 단계 분류
- `pickWeakestElement(elements)` — 가장 약한 오행 1개
- `buildPrescriptionSet(weak, age, seed)` — 6카드 결정론 도출

**프롬프트 교체** [route.ts:1314+](app/api/generate/route.ts:1314):
- 매트릭스 결과를 그대로 박아 넣고 "글자 하나도 바꾸지 말고 그대로 출력" 강제
- 일반 양육 상식·임의 추가 절대 금지

**예시 (火 부족, 유아):**
```
🌞 즉효 처방 — 햇볕 자리에서 까꿍·간지럽히기 1분
🎈 일상 처방 — 매일 따뜻한 색 풍선·공으로 놀이
😶 피해야 할 결 — 표정 없이 무뚝뚝하게 응대하기
🟧 공간 — 주홍·노랑 포인트 쿠션·소품
🟡 감각 — 따뜻한 색 그림책·모빌
☀️ 리듬 — 여름·맑은 날 외부 활동 늘리기
```

---

## 10. 절제 회로(土) 점수/부제 모순 수정

### 10.1 문제
- 막대 100% 가득 차 있는데 부제는 "49%"
- 두 수치가 완전히 다른 식으로 계산되어 모순처럼 보임

### 10.2 진단 ([parent-child-charts.ts:484](lib/parent-child-charts.ts:484))
| 지표 | 식 | 결과 |
|---|---|---|
| 막대 점수 | (관성*1.2 + 토*0.6) / 6 * 100, **cap 100** | 320 → 100 |
| 부제 % | 토 / max오행 * 100 | 49% |

### 10.3 해결 (B+A)
**(B) 점수 식 재조정:**
```ts
const restraintScore = Math.min(100, Math.round(
  (Math.min(gwansong, 5) / 5) * 50 + (Math.min(toMax, 50) / 50) * 50
));
```
관성(0~5)과 토 비율(0~50%)을 50/50으로 합쳐 자연스러운 0~100.

**(A) 부제 = 점수 통일:**
```ts
subtitle: `"멈출 수 있는 힘" — ${restraintScore}%`
```

→ 이번 가족 (관성 4 + 토 24%) 결과: 막대·부제 모두 **64%**.

### 10.4 양인살(羊刃殺) 설명
- 명리 신살 — "刃"(칼날)
- 결단력·돌파력 (긍정) / 격발성·폭발 (부정) 양면
- 코드는 양인살 + 괴강살 활성 시 82점 고정, 비활성 시 비겁 카운트 기반 잠재 점수

---

## 11. 메모리 영구 규칙

[`C:\Users\PC\.claude\projects\C--Users-PC-Desktop-saju\memory\`](C:\Users\PC\.claude\projects\C--Users-PC-Desktop-saju\memory\)

- `feedback_no_force_push.md` — saju 프로젝트는 일반 `git push`만 사용. 거부되면 pull --rebase로 해결. force-push 절대 금지.
- `MEMORY.md` — 위 규칙 인덱스

---

## 12. 최종 변경 파일 목록

### 신규 생성
- `lib/opener-seed.ts` — 첫마디 결정론 시드 + 분포 분류
- `lib/text-postprocess.ts` — 자녀 호칭 후처리 안전망
- `lib/element-prescription.ts` — 270 처방 매트릭스
- `lib/wording.ts` (워크트리에 추가, 어휘·헬퍼)

### 수정
- `app/api/generate/route.ts` — 시드 호출, 분포 컨텍스트, 호칭 강화, 처방 매트릭스 호출, 클리셰 금지 규칙
- `app/page.tsx` — 엄마와 아이 궁합 카드 추가
- `components/ParentChildSlideResult.tsx` — 한 줄 카피 호출, 호칭 후처리, 차트 절대 스케일, 방사형 막대, 재성 라벨
- `lib/parent-child-charts.ts` — 십성 5분류 라벨(재성=돈·물건의 결), 절제 회로 식

---

## 13. 최종 배포 워크플로우 (권장)

```bash
cd C:/Users/PC/Desktop/saju
npx vercel --prod --yes
```

- 약 40~60초 소요
- 완료 후 paljawon.com 반영
- Vercel 대시보드 → saju 프로젝트 → Deployments에서 진행 상황 확인 가능

**주의:**
- 다른 PC에서 작업 시 같은 vercel 계정(tooxx3-1879) 로그인 필요
- 코드는 git push로 GitHub 백업하되, **Vercel 배포는 CLI**가 신뢰성 높음
- force push 절대 금지

---

## 14. 다음 작업 시 참고

- 이 매트릭스/시드/후처리는 **부모-자녀 페이지에만** 적용. 연인 사주(matching)·평생 사주는 별개 프롬프트 사용 중
- 같은 패턴을 연인·평생 사주에도 확장하면 일관된 사주 근거 강화 가능 (사용자 미요청)

---

**대화 종료 시점:** 2026-04-30
**최종 배포:** `saju-44l2w7kp4` (Ready)
